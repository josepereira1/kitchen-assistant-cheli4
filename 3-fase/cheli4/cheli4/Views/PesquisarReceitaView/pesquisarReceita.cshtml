@model cheli4.Models.Comercial.Receita;
@{
    ViewData["Title"] = "pesquisarReceita";
}

<div class="row">
    <div class="col-md-4">
        <img src="~/imagens/receita.PNG" />
        <br /><br />
        <img src="~/imagens/relogio.png"> Preparação @Model.duracao min</>
        <!--<form asp-action="pesquisarReceita">

        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="form-group">
            <label asp-for="nome" class="control-label">Nome da receita:</label>
            <input asp-for="nome" class="form-control" />
            <span asp-validation-for="nome" class="text-danger"></span>
        </div>

        <div class="form-group">
            <input type="submit" value="Pesquisar" class="btn btn-default btn-primary" />
        </div>

    </form>-->
            <ul>
                <li>Nome: @Model.nome</li>
                <li>Tipo: @Model.tipo</li>
                <li>Duração: @Model.duracao min</li>
                <li>Gastronomia: @Model.gastronomia</li>
                @if (Model.disponivel == 1)
                {
                    <li>Disponibilidade: Disponível</li>
                }
                else
                {
                    <li>Disponibilidade: Indisponível</li>
                }
            </ul>
            <br />
            <h4>Ingredientes:</h4>
    </div>
</div>


